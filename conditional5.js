// Operador ternario
const idade = 17

idade >= 18 ? console.log('Adulto') : console.log('Menor de idade')

// Funcao utilizando operador ternario
const somaOuMultiplica = (valor) => {
  return valor >= 10 ? valor + valor : valor * valor;
}